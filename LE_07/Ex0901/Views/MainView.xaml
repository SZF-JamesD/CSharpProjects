<UserControl x:Class="Ex0901.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ex0901.Views"
      xmlns:helpers="clr-namespace:Ex0901.Helpers"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#2C2C2C">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="81*"/>
            <RowDefinition Height="122*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Weather Today" Foreground="#FFE4E4E4" FontSize="18" FontWeight="Bold" Margin="10,10,10,10" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="10,10,10,10">
            <TextBlock FontSize="14" Foreground="#FFC1CC" Text="{Binding Temperature}"/>
            <TextBlock Text="{Binding WeatherDescription}" FontSize="14" Foreground="#B39CD0"/>
            <TextBlock Text="{Binding Location}" FontSize="14" Foreground="#A8DADC"/>
            <TextBlock Text="{Binding AirQuality}" FontSize="14" Foreground="#FFC1CC"/>
        </StackPanel>
        <TextBox x:Name="SearchBox" Text="{Binding SearchCriteria, UpdateSourceTrigger=PropertyChanged}" 
        helpers:WatermarkService.Watermark="City or Coordinates" Background="#E4E4E4" 
        Margin="5,0,0,5" Grid.Row="1" HorizontalAlignment="Left" Width="239" VerticalAlignment="Bottom" Height="32"/>

        <Button Content="Search" Command="{Binding SearchCommand}" Background="#B39CD0" Height="32" Width="156" Grid.Row="1" Grid.Column="0" 
                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="248,0,0,5" />

        <TextBlock Text="5-Day Forecast" Foreground="#FFE4E4E4" FontSize="18" FontWeight="Bold" Margin="10,10,10,9" Grid.Row="0" Grid.Column="1"/>

        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Visible" Margin="0,0,1,5" Grid.RowSpan="2">
            <ItemsControl Margin="10,10,10,10" ItemsSource="{Binding GroupedForecasts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding DateString}" Margin="5" Foreground="#E4E4E4" FontWeight="Bold" BorderThickness="0">
                            <ItemsControl ItemsSource="{Binding ForecastsCollection}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="10">
                                            <TextBlock Text="{Binding TimeString}" FontSize="14" Foreground="#E4E4E4" FontWeight="Bold"/>
                                            <TextBlock Text="Temp: " FontSize="14" Foreground="#FFC1CC">
                                                            <Run Text="{Binding FutureTemperature}"/>
                                                            <Run Text="C"/>
                                            </TextBlock>
                                            <TextBlock Text="Description: " FontSize="14" Foreground="#B39CD0" Margin="0,0,0,10">
                                                            <Run Text="{Binding FutureWeatherDescription}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ItemsControl ItemsSource="{Binding TileGrid}" Grid.Column="0" Grid.Row="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Image Source="{Binding MapImage}" Stretch="Fill" Opacity="0.8"/>
                        <Image Source="{Binding WeatherImage}" Stretch="Fill" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
