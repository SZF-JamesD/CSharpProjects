<UserControl x:Class="Ex0603.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid Background="White">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Enter Employee ID:" Grid.Row="0" Margin="0,0,0,10"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Height="25" Margin="0,0,0,10"/>
            <TextBlock Text="Enter Password:" Grid.Row="2" Margin="0,0,0,10"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Height="25" Margin="0,0,0,10"/>
            <Button Content="Login" Grid.Row="4" Height="30" Command="{Binding LoginCommand}"/>
        </Grid>
    </Grid>
</UserControl>

