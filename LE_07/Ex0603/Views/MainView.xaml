<UserControl x:Class="Ex0603.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    
    <Grid Background="White" Margin="20,20,20,20">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0"  Grid.ColumnSpan="2" Content="Invoice Manager" FontSize="24" UseLayoutRounding="False"/>

        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <Label Grid.Row="0" Grid.Column="0" Content="Customer Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"/>


            <Label Grid.Row="2" Grid.Column="0" Content="Customer Number:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="0" Text="{Binding CustomerNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Product Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="5" Grid.Column="0" Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="6" Grid.Column="0" Content="Product Price:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="7" Grid.Column="0" Text="{Binding ProductPrice, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="8" Grid.Column="0" Content="Quantity:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="9" Grid.Column="0" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Create Invoice" Command="{Binding CreateInvoiceCommand}" Grid.Row="11" Grid.Column="0" Width="120" Height="35" Margin="5"/>

        </Grid>

        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <Label Grid.Row="0" Grid.Column="0" Content="Invoices"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible">
                <ListView x:Name="InvoiceListView" MouseDoubleClick="InvoiceListView_MouseDoubleClick" ItemsSource="{Binding Invoices}" SelectedItem="{Binding SelectedInvoice, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding InvoiceDate, StringFormat='{}{0:d}'}" Margin="0,0,10,0"/>
                                <TextBlock Text="Customer: " FontWeight="Bold" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Customer.CustomerNumber}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

